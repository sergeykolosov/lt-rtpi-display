[project]
name = "pi-rtpi-display"
version = "0.1.0"
description = "Real-time public transport departure board for Raspberry Pi TTY"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]
